import { NutrientWeightPipe } from './nutrient-weight.pipe';

describe('NutrientWeightPipe', () => {
  it('create an instance', () => {
    const pipe = new NutrientWeightPipe();
    expect(pipe).toBeTruthy();
  });
});
