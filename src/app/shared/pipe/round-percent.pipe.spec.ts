import { RoundPercentPipe } from './round-percent.pipe';

describe('RoundPercentPipe', () => {
  it('create an instance', () => {
    const pipe = new RoundPercentPipe();
    expect(pipe).toBeTruthy();
  });
});
